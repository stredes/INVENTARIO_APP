# Variables de entorno para la API
# Copia a .env y ajusta

# PostgreSQL recomendado para despliegue
# DATABASE_URL=postgresql+psycopg2://usuario:password@host:5432/inventario?sslmode=prefer

# Si no defines DATABASE_URL, se usará SQLite local según src/data/database.py

